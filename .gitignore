# 1. 先忽略根目录下的所有内容
*

# 2. 允许同步的文件和文件夹（白名单）
!.gitignore
!backend/
!sora/
!requirements.txt

# 3. 排除特定文件和文件夹（即使它们在 backend 目录内）
# 忽略任何位置下的 .env 文件
**/.env

# 排除后端本地数据库
backend/db.sqlite3

# 排除后端媒体文件上传目录
backend/media/

# 附加：忽略 Python 和前端生成的缓存/依赖
**/__pycache__/
**/*.pyc
**/node_modules/
.DS_Store